
// This is a minimal file since we're using plain HTML, CSS, and JavaScript
// The actual application is loaded directly from index.html

import { createRoot } from 'react-dom/client'

// Empty render since we're using vanilla HTML/JS
createRoot(document.getElementById("root")!).render(null);
